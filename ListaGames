<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" sizes="16x16"  href="./Imagens/favicon-16x16.png">
    <meta name="msapplication-TileColor" content="#ffffff">
    <meta name="theme-color" content="#ffffff">
    <title>Jogos Antigos</title>
</head>
<body>

    <!--<iframe src='' frameborder='0' allowfullscreen></iframe>-->

    <div id="game">
        
    </div>

    <form>
        <label for="color">Favorite color?</label><br>
        <button onclick="selecttion()">Botão</button>
        <select id="colors">
            <option value="select">-Select-</option>
            <option value="red">Red</option>
            <option value="gray">Gray</option>
            <!--<option value="blue">Blue</option>
            <option value="black">Black</option>
            <option value="white">White</option>
            <option value="green">Green</option>
            <option value="yellow">Yellow</option>
            <option value="orange">Orange</option>
            <option value="purple">Purple</option>-->
        </select>
    </form>

    <script>
        function selecttion(){
            let colors = document.getElementById("colors").value;
            let game = document.getElementById("game");
            
            //console.log(colors);
            
            if (colors == "red"){
                game.innerHTML = `<iframe src='https://www.minijogos.com.br/embed/super-bomberman-4' frameborder='0' allowfullscreen></iframe>`
            
            };
            if (colors == "gray"){
                game.innerHTML = "<iframe src='https://www.minijogos.com.br/embed/bomberman-fantasy-race' frameborder='0' allowfullscreen></iframe>"
            };
            /*
            if (colors == "blue"){
                alert(colors)
            };
            if (colors == "black"){
                alert(colors)
            };
            if (colors == "white"){
                alert(colors)
            };
            if (colors == "green"){
                alert(colors)
            };
            if (colors == "yellow"){
                alert(colors)
            };
            if (colors == "orange"){
                alert(colors)
            };
            if (colors == "purple"){
                alert(colors)
            };
            */
        }
    </script>

    <style>
        body {
            background-color: black;
        }
        button {
            background-color: brown;
            color: aliceblue;
        }
        select {
            background-color: brown;
            color: aliceblue;
        }
        iframe {
            width:100%;
            height:760px;
        }
    </style>
</body>
</html>
